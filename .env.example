# Firebase Client Config (Required)
# Get these from Firebase Console > Project Settings > General > Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=off-the-yard.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=off-the-yard
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=off-the-yard.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Admin SDK (Optional - only needed if not using JSON file)
# Option 1: Use the service account JSON file (recommended for local dev)
# Place off-the-yard-firebase-adminsdk-*.json in project root
# The app will automatically detect and use it.

# Option 2: Use environment variables (recommended for production)
# FIREBASE_PROJECT_ID=off-the-yard
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@off-the-yard.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n

